# Imprint: keep sleep, but make deep sleep very rare

CONFIG_ZMK_IDLE_TIMEOUT=30000          # 30초 후 idle 진입 (필요시 조정)
CONFIG_ZMK_SLEEP=y                     # 딥슬립은 켬
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000  # 30분 후에만 딥슬립 (1800000ms)
